\documentclass[14pt]{extarticle}

\usepackage[T2A]{fontenc}
\usepackage[cp1251]{inputenc}
\usepackage[russian]{babel}

\usepackage{graphicx}

\usepackage{amsmath, amsthm, amsfonts, amssymb}
\usepackage[unicode, hidelinks]{hyperref}

\usepackage[
    a4paper, mag=1000,
    left=2.5cm, right=1.5cm, top=2cm, bottom=2cm, bindingoffset=0cm,
    headheight=0cm, footskip=1cm, headsep=0cm
]{geometry}

\usepackage{tempora}

\usepackage{chngcntr} % equation counter manipulations

% reset equation counter at each section
\counterwithin*{equation}{section}
\counterwithin*{equation}{section}

\graphicspath{{images/}}

\usepackage{cases}

\begin{document}

\tableofcontents

\newpage

\section{Предельные теоремы и законы больших чисел}

Рассмотрим последовательность $\{\xi_i\}^{\infty}_{i = 1}$ случайных величин
с $M\xi_i < \infty$.

\subsection{Сходимость последовательностей случайных величин (по вероятности,
почти наверное, в среднем, по распределению)}

\textbf{Определение:}
\smallskip

Последовательность случанйых величин $\{\xi_i\}^{\infty}_{i = 1}$ сходится по вероятности
к случайной величине $\xi$ $(\xi_i \displaystyle\rightarrow^{P} \xi)$, если
$\forall \epsilon > 0$

$$P\{\omega : |\xi_i(\omega) - \xi(\omega)| > \epsilon\} \displaystyle\rightarrow_{i \to \infty} 0$$

или 

$$P\{\omega : |\xi_i(\omega) - \xi(\omega)| < \epsilon\} \displaystyle\rightarrow_{i \to \infty} 1$$

\textbf{Определение:}
\smallskip

Последовательность случанйых величин $\{\xi_i\}^{\infty}_{i = 1}$ сходится почти наверное
к случайной величине $\xi$ $(\xi_i \displaystyle\rightarrow^{\text{П.Н.}} \xi)$,
если 

$$P\{\omega: \xi_i(\omega) \rightarrow \xi(\omega)\} = 1$$

или 

$$P\{\omega: \xi_i(\omega) \nrightarrow \xi(\omega)\} = 0$$

\textbf{Определение:}
\smallskip

Последовательность случанйых величин $\{\xi_i\}^{\infty}_{i = 1}$ сходится по распределению
к случайной величине $\xi$, если последовательность функций распределения $F_{\xi_i}(x)$
слабо сходится к $F_{\xi}(x)$.

То есть

$$F_{\xi_i}(x) \displaystyle\rightarrow^{\omega} F_{\xi}(x) \Rightarrow
P\{\omega: \xi_i(\omega) < x\} \rightarrow P\{\omega:\xi(\omega)<x\}$$

\textbf{Определение:}
\smallskip

Последовательность случанйых величин $\{\xi_i\}^{\infty}_{i = 1}$ сходится в среднем порядка $p$
к случайной величине $\xi$, если $M|\xi_i - \xi|^{p}\displaystyle\rightarrow_{i \to \infty} 0$.
\smallskip

\subsection{Теорема о связи между сходимостью в среднем и по вероятности}

\textbf{Теорема:}
\smallskip

Из сходимости среднеквадратичного следует сходимость по вероятности.
\bigskip

Доказательство:
\begin{center}
    \includegraphics[width=170mm]{1_1_4.png}
\end{center}

\subsection{Теорема о связи между сходимстью по вероятности и сходимости к константе}

\textbf{Теорема:}
\smallskip

Пусть $(\xi_n \displaystyle\rightarrow^d \xi)$. Тогда $(\xi_n \displaystyle\rightarrow^P \xi)$.
\bigskip

Доказательсво:
\begin{center}
    \includegraphics[width=170mm]{1_1_1.png}
\end{center}

\subsection{Теорема о связи между сходимостями по вероятности, в среднем, по
распределению}

\textbf{Теорема:}
\smallskip

Из сходимости почти наверное вытекает сходимость по вероятности $\{\xi_n\}
\displaystyle\rightarrow^{\text{п.н.}} \xi \Rightarrow \{\xi_n\}
\displaystyle\rightarrow^P \xi$.
\smallskip

Из схоимости по вероятности вытекает сходимость по распределению $\{\xi_n\}
\displaystyle\rightarrow^P \Rightarrow \{\xi_n\} \displaystyle\rightarrow^d \xi$.
\smallskip

Из сходимости в среднем по порядку вытекает сходимость по вероятности
$\{\xi_n\} \rightarrow \xi \Rightarrow \{\xi_n\} \displaystyle\rightarrow^P \xi$.
\smallskip

\subsection{Определение закона больших чисел. Теорема ЗБЧ для н.о.р. величин (с доказательством
с помощью х.ф.)}

\textbf{Определение:}
\smallskip

Пусть $\{\xi_i\}^{\infty}_{i = 1}$~--- последовательность случайных величин с
$M|\xi_i| < + \infty$. Обозначим $M\xi_i = a < +\infty$.
\bigskip

Говорят, что последовательность $\xi_i$ удовлетворяет закону больших
чисел, если $\frac{S_n}{n} \rightarrow \frac{A_n}{n}$

$S_n = \xi_1 + \dots + \xi_n; \;\;\; A_n = a_1 + \dots a_n$.

То есть среднее арифметическое из наблюдений сходится к среднему из математического
ожидания.
\bigskip

\textbf{Теорема ЗБЧ для независимых одинаково распределённых случайных величин:}
\smallskip

Пусть $\{\xi_i\}$~--- независимы одинаково распределённые случаные величины с
$M|\xi_i| = M|\xi_1| < + \infty \;\;\; \forall i$. Тогда $\{\xi_i\}$ удовлетворяют
закону больших чисел.
\bigskip

Доказательство:
\begin{center}
    \includegraphics[width=170mm]{1_1_2.png}
\end{center}

\subsection{Центральная предельная теорема}

\textbf{Теорема:}
\smallskip

Пусть $\{\xi_i\}$~--- последовательность независимых одинаково распределённых
случайных величин с $M|\xi_i| < + \infty$, $M\xi_1 = a; \;\;\; D\xi_1 = \sigma^2$.
Тогда $\forall x \in R \;\;\; P\left\{\frac{S_n - na}{\sqrt{DS_n}} < x\right\}
\displaystyle\rightarrow_{n \to \infty} \frac{1}{\sqrt{2 \pi}} \in^x_{-\infty}
e^{-\frac{n^2}{2}} du$. То есть $z_n = \frac{S_n - \mu S_n}{\sqrt{DS_n}}$ сходится
по распределению с случаной величиной $z \sim N(0, 1)$.
\bigskip

Доказательство:
\begin{center}
    \includegraphics[width=170mm]{1_1_3.png}
\end{center}

\section{Вариационные ряды и их характеристики}

\subsection{Генеральная и выборочная совокупности, объём выборки.}

Рассмотрим постановку задачи математической статистики: по результатам
наблюдения за некоторой случайной величиной $\xi$ требуется сделать
выводы о неизвстном законе распределения этой величины $\mathcal{L}(x,\theta)$
либо о неизвестных парамерах $\theta_1, \dots, \theta_n$ известного распределения.
\bigskip

Пусть $\xi$~--- случайная величина с некоторой (теоретической) функцией распределения
$F_{\xi}(x) = P\{\xi < x\}, \;\;\; x \in R.$
\bigskip

\textbf{Определение:}
\smallskip

Совокупность $n$ независимых одинаково распределённых случайных величин $X_1,
X_2, \dots, X_n$ называется выборкой (выборочной совокупностью),
извлечённой из распределения случайной величины $\xi$.
\bigskip

\textbf{Определение:}
\smallskip

Под генеральной совокупностью понимается множество всех возможных значений случайной
величины $\xi$.
\bigskip

\textbf{Определение:}
\smallskip

Объёмом совокупности называется количество всех её элементов, объём выборки или
выборочной совокупности обозачается $n$, генеральной совокупности~--- $N$.
\bigskip

\subsection{Варианционный ряд, варианта, частота. Виды вариационных рядов.
Гистограмма, полигон.}

\textbf{Определение:}
\smallskip

Вариационный ряд~--- это последовательность расположенных в порядке неубывания
результатов наблюдения $x^{*}_1 \leq \dots \leq x^{*}_n$.
\smallskip

$x_i^{*}$~--- варианта.
\smallskip

$n_i$~--- частота появления варинты $x_i^{*}$ в выборке.
\bigskip

\textbf{Определение:}
\smallskip

Точечным вариационным рядом называется:

\begin{center}
    \includegraphics[width=150mm]{toch_var_rad_1.PNG}
\end{center}

$x_i$~--- варианта, $n_i$~--- частота соответствуующей варианты.
\smallskip

$m$~--- количество групп (различных вариант (вариант в таблице)).
\smallskip

$n = \displaystyle\sum^m_{i = 1} n_i$, где $n$~--- объём выборки.
\bigskip

Для графического представления точечных вариационных рядов используется полигон
частот~--- ломанная с вершинами в точках $(x_i, n_i)$.

\begin{center}
    \includegraphics[width=120mm]{toch_var_rad_2.PNG}
\end{center}

\textbf{Определение:}
\smallskip

Интервальным вариационным рядом называется:

\begin{center}
    \includegraphics[width=150mm]{interval_var_rad_1.PNG}
\end{center}

$x_i$~--- варианты, $n_i$~--- частота.
\smallskip

$m$~--- количество групп (интервалов).
\smallskip

$n = \displaystyle\sum^m_{i = 1} n_i$, где $n$~--- объём выборки.
\bigskip

Для графического представления интервальных вариационных рядов используется гистограмма
частот~--- фигура, составленная из прямоугольников, одной стороной которых служат
интервалы $(x_i, x_{i + 1}]$, а длина второй равна $n_i$.

\begin{center}
    \includegraphics[width=120mm]{interval_var_rad_2.PNG}
\end{center}

\subsection{Формулы числовых характеристик. Эмперическая функция распределения
(ЭФР). Свойства ЭФР.}

\textbf{Определение:}
\smallskip

Выборочным средним называется величина:

$$\overline{x} = \frac{1}{n} \sum^n_{i = 1} \mathbf{X_i}.$$

Если данные представлены в виде точечного или интервального вариационного ряда,
то для вычисления используют формулу:

$$\overline{x} = \frac{1}{n} \sum^n_{i = 1} x_i * n_i,$$

где $m$~--- количество групп в точечном или интервалов в интервальном вариационном
ряду, $n_i$~--- частота, т.е. количество элементов выборки, принадлежащихх $i$"=той
группе или $i$"=тому интервалу, $x_i$~--- варианта для точечного ряда и середина
$i$"=того интервала для интервального ряда.

\textbf{Определение:}
\smallskip

Выборочной дисперсией (смещённой) называется величина:

$$S^2 = \frac{1}{n} \sum^n_{i = 1} (X_i - \overline{x})^2.$$

Она характеризует среднее из квадратов отклонений наблюдаемой величины от выборочного
среднего. Величина $S = \sqrt{S^2}$ называется выборочным средним квадратическим
отклонением (смещённым) величин выборки от выборочного среднего.
\smallskip

Если данные представлены в виде точечного или интервального вариационного ряда, то для
вычисления используют формулу:

$$S^2 = \frac{1}{n} \sum^m_{i = 1} (x_i - \overline{x})^2 n_i.$$

или

$$S^2 = \left(\frac{1}{n} \sum^m_{i = 1}x^2_i n _i\right) - \overline{x}^2.$$

Здесь $m$~--- количество групп в точечном или интервалов в интервальном вариационных
рядах, $n_i$~--- частота. т.е. количество элементов выборки, принадежащих
$i$"=той группе или $i$"=тому интервалу, $x_i$~--- варианта для точечного ряда и
середина $i$"=того интервала для интервального ряда.
\bigskip

\textbf{Определение:}
\smallskip

Выборочной дисперсией (несмещённой) называется величина:

$$\overline{\sigma} = \frac{1}{n - 1} \sum^n_{i = 1} (X_i - \overline{x})^2.$$

Аналогично, величина $\overline{\sigma} = \sqrt{\overline{\sigma}^2}$ называется
выборочным несмещённым средним квадратическим отклонением.
\smallskip

Очевидно, что смещённая и несмещённая выборочные дисперсии связаны формулой:

$$\overline{\sigma}^2 = \frac{n}{n - 1} S^2.$$

\subsection{Эмперическая функция распределения ЭФР. Свойства ЭФР.}

\textbf{Эмперическая функция распределения ЭФР}
\smallskip

Эмперической функцией распределения (ЭФР) называется функция

$$\tilde{F}_n(x) = \frac{1}{n} \sum^n_{i = 1} e (x - X_i),$$

где $e(x) = 1$, при $x > 0 \;\;\; e(x) = 0$, при $x \leq 0$.
\smallskip

Таким образом, если $X_i < x$, то $e(x) = 1$, если $X_i \geq x$,
то $e(x) = 0$, а сумма $e(x - X_i)$ будет равна количеству элементов выборки,
которые приняли значение, строго меньше некоторого $x \in R$.
\smallskip

Пусть $x_1, x_2, \dots, x_n$~--- реализация выборки $X_1, X_2,
\dots, X_n$, т.е. наблюдавшиеся значения случайной величины $\xi$.
\smallskip

Обозначим $\mu(x)$~--- число элементов выборки, строго меньших $x \in R$.
Тогда эмпирическая функция распределения $\tilde{F}_n(x)$ может быть определена
как

$$\tilde{F}_n(x) = \frac{\mu(x)}{n}.$$
\bigskip

\textbf{Свойства ЭФР:}

\begin{enumerate}
    \item $0 \leq \tilde{F}_n(x) \leq 1,$ т.к. $0 \leq \mu(x) \leq n$;
    \item неубывающая непрерывная слева функция;
    \item $\tilde{F}_n(x)$~--- ступенчатая функция для всех типов распределений;
    \item $\tilde{F}_n(x)$ сходится по распределению к $F_{\xi}(x)$.
\end{enumerate}

\section{Оценки параметров распределения}

\subsection{Понятия статистики, оценки, выборочной характеристики.}

\textbf{Определение:}
\smallskip

Пусть $g(t_1, \dots, t_n)$~--- непрерывная функция. Оценкой $\theta$ назовём
$\tilde{\theta} = g (X_1, \dots, X_2)$.

Если $g(X_1, \dots, X_n) = T$ некоторая функция, то
$T$~--- статистика.
\bigskip

\textbf{Определение:}
\smallskip

Выборочными характеристиками называются функции от наблюдений (точечные оценки),
приближённо оценивающие соответствующие числовые характеристики случайной величины.
\bigskip

\subsection{Несмещённые, состоятельные и эффективные оценки}

\textbf{Определение:}

Оценка $\tilde{\theta}_n$ называется несмещённой оценкой параметра $\theta$,
если $M(\tilde{\theta}_n) = \theta$.
\bigskip

\textbf{Определение:}
\smallskip

Оценка $\tilde{\theta}_n$ называется состоятельной оценкой параметра $\theta$,
если $\tilde{\theta}_n$ сходится по вероятности к $\theta$.
\bigskip

\textbf{Определение:}
\smallskip

Оценка $\tilde{\theta}_n$ называется эффективной, или оптимальной, или наилучшей
несмещённой оценкой с минимальной дисперсией (НОМД), если $M(\tilde{\theta}_n) =
\theta$ и $D(\tilde{\theta}_n) = \displaystyle\inf_{\tilde{\theta}^{*}_n} D \tilde{\theta}^{*}_n$.
\bigskip

\subsection{Теорема о несмещённой состоятельной оценке мат. ожидания}

Пусть $\xi \sim L(x, \theta)$, $L(x, \theta)$~--- закон распределения известен
с точностью до параметра.

$$\overline{\theta} = (\varTheta_1, \dots, \varTheta_n)$$

По результатам $X_1, \dots, X_n$ наблюдений за $\xi$ требуется
построить оценку $\theta$.
\bigskip

\textbf{Теорема:}
\smallskip

Пусть $X_1, \dots, X_n \sim L_{\xi} (x, \theta)$, где
$\xi$~--- случайная величина с $M\xi = a < +\infty \;\;\; D\xi = \sigma^2$.
Тогда выборочное среднее $\overline{x} = \frac{1}{n} \sum^n_{i = 1} x_i$
является несмещённой и состоятельной оценкой $M\xi$.
\bigskip

Доказательство:
\bigskip

$$M\overline{x} = M\left(\frac{1}{n} \sum^n_{i = 1} x_i\right) = \frac{1}{n} \sum^n_{i = 1}
Mx_i =$$ 
$$= \left|x_i \text{~--- н. о. р. случайной величины} \Rightarrow
Mx_i = M\xi \;\;\; \forall i\right| = \frac{1}{n} \sum^n_{i = 1} M\xi =$$
$$= \frac{1}{n} * n * a = a \Rightarrow \overline{x}\text{~--- несмещённое}$$.

Состоятельность $\forall \epsilon > 0 \;\;\; P\{|\overline{x} - a| < \epsilon\}
\displaystyle\to_{n \to \infty} 1$
\bigskip

По неравенству Чебышёва:

$$\forall \epsilon > 0 \;\;\; P\{|\overline{x} - a| < \epsilon\} \geq 1 -
\frac{D\overline{x}}{\epsilon^2} = 1 - \frac{1}{\epsilon^2} D \left(
\frac{1}{n} \sum^n_{i = 1} x_i\right) =$$

$$=\left|\text{н. о. р.} \;\;\; Dx_i = \sigma^2\right| =$$

$$= 1 - \frac{1}{n^2}{\epsilon^2} D\xi = 1 - \frac{n \sigma^2}{n^2 \epsilon^2} =
1 - \frac{\sigma^2}{n \epsilon^2} \rightarrow_{n \to \infty} 1 - 0$$

Т.к. $P(A) \leq 1 \;\;\; \forall A$, то $\lim_{n \to \infty} P\{|\overline{x} - a| <
\epsilon\} = 1 \Rightarrow \overline{x}$~--- состоятельная.

\subsection{Дополнительная информация}

\textbf{Определение:}
\smallskip

Выборочной дисперсией (смещённой) называется величина $S^2 = \frac{1}{n} \sum^n_{i = 1}
(x_i - \overline{x})^2$.

Для группированных данных: $S^2 = \frac{1}{n} \sum^m_{i = 1}
(x_i - \overline{x})^2 * n_i$.
\bigskip

\textbf{Определение:}
\smallskip

Выборочной дисперсией (несмещённой) называется оценка $\tilde{\sigma}^2 = \frac{1}{n - 1}
\sum^n_{i = 1} (x_i - \overline{x})^2$.

Для группированных данных: $\tilde{\sigma}^2 = \frac{n}{n - 1} * S^2$.
\bigskip

\subsection{Теорема о несмещённой и состоятельной оценке дисперсии}

\textbf{Теорема:}
\smallskip

Пусть $X_1, \dots, X_n \sim L(x, \theta)$, где $M\xi = a < +\infty$,
$D\xi = \sigma^2$. Тогда несмещённой и состоятельной оценкой $D\xi$ является величина
$\tilde{\sigma}^2 = \frac{1}{n - 1} \sum^n_{i = 1} (x_i - \overline{x})^2$.
\bigskip

Доказательство:
\begin{center}
    \includegraphics[width=160mm]{2_3_1.png}
\end{center}

\subsection{Теорема о несмещённой и состоятельной оценке функции распределения}

\textbf{Определение:}
\begin{center}
    \includegraphics[width=170mm]{2_3_2.png}
\end{center}

\textbf{Теорема:}
\smallskip

Пусть $X_1, \dots, X_n \sim L_{\xi}(x, \theta)$, где
$F_{\xi}(x)$~--- функция распределения случайной величины $\xi$. Тогда
эмперическая функция распределения $\tilde{F_n}(x)$ является несмещённой и состоятельной
оценкой функции распределения $F_\xi(x)$.
\bigskip

Доказательство:
\begin{center}
    \includegraphics[width=170mm]{2_3_3.png}
\end{center}

\subsection{Теорема об эффективной оценке математического ожидания}

\textbf{Определение:}
\smallskip

Пусть $\tilde{\theta}_n$~--- несмещённая оценка. $\tilde{\theta}_n$~--- называется
эффективной оценкой, если

$$D\tilde{\theta}_n =
\displaystyle\inf_{\tilde{\theta}^{*}_n} D\tilde{\theta}^{*}_n$$

\textbf{Теорема:}
\smallskip

Пусть $X_1, \dots, X_n \sim L_{\xi}(x, \theta)$, где $\xi$~---
случайная величина с $M\xi = a < + \infty$. Тогда выборочное среднее
$\overline{x} = \frac{1}{n} \sum^n_{i = 1} X_i$ является эффективной
оценкой $M\xi$ в классе линейных несмещённых оценок.
\bigskip

Доказательство:
\begin{center}
    \includegraphics[width=170mm]{2_3_4.png}
\end{center}

\subsection{Теорема о единственности эффективной оценки}

\textbf{теорема:}
\smallskip

Пусть $X_1, \dots, X_n \sim L_{\xi}(x, \theta)$, где $\theta$~---
параметр распределения, и пусть $\tilde{\theta}(\overline{X}_n)$ и
$\widehat{\theta}_n(\overline{X}_n)$~--- две эффективные оценки $\theta$. Тогда
$\tilde{\theta}_n(\overline{X}_n) = \widehat{\theta}_n(\overline{X}_n)$.
То есть $P\{(x_1, \dots, x_n) : \tilde{\theta}_n(\overline{X}_n) \neq
\widehat{\theta}_n(\overline{X}_n)\} = 0.$
\bigskip

Доказательство:
\begin{center}
    \includegraphics[width=170mm]{2_3_5.png}
\end{center}

\subsubsection{Определение регулярной параметрической модели. Информация Фишера.}

\textbf{Определение:}
\smallskip

Пусть $\xi$~--- наблюдаемая случайная величина.

Выборочным пространством $\mathbf{X}_n$
называется $\{\overline{X}_n = (X_1, \dots, X_n)\}$.

Параметрическим множеством называется $\varTheta$~--- множество значений параметра
$\theta$.
\bigskip

\textbf{Определение:}
\smallskip

Параметрической моделью называется

$$\left\{\mathbf{X}_n; F_{\xi}(x, \theta) : \theta \in \varTheta\right\}$$
\bigskip

\textbf{Определение:}
\smallskip

Параметрическая модель называется регулярной, если:
\begin{enumerate}
    \item параметрическое множество $\varTheta$~--- открытое множество;
    \item {носитель распределён, то есть множество $A = \{x : f(x) > 0\}$~---
    не зависит от параметров;}
    \item {$\forall \theta \;\; \text{и} \;\; x \in A$ существует производная
    $\frac{\partial f(x, \theta)}{\partial \theta} < +\infty$;}
    \item {$\forall \theta \in \varTheta \;\;\; M\left(\frac{\partial \ln f(x, \theta)}{\partial \theta}\right)
    = 0 \;\;\; M\left(\frac{\partial \ln f(x, \theta)}{\partial \theta}\right)^2 < +\infty$;}
    \item {Дважды допустимо дифференцирование под знаком интеграла по парамету.}
\end{enumerate}

\textbf{Определение:}
\smallskip

Пусть $\xi$~--- случайная величина с функцией плотности $f(x,\theta)$. Тогда
информацией Фишера для случайного вектора $\overline{X}_n$ называется
$I_n(\theta) = M(\frac{\partial}{\partial \theta} \ln f (x, \theta))^2$
\bigskip

\subsection{Неравенство Рао-Крамера. Построение эффективной по Рао-Крамеру
оценки для пуассоновского распределения}

\textbf{Неравенство Рао"=Крамера:}
\smallskip

Пусть дана регулярная параметрическая модель. Тогда для любой несмещённой оценки
$\tilde{\theta}$ параметра $\theta$ $D\tilde{\theta} \geq \frac{1}{n I_n (\theta)}$.
\bigskip

\textbf{Определение:}
\smallskip

Величину $e(\theta) = \frac{1}{n I(\theta) * D \tilde{\theta}}$ называют показаелем
эффективности по Рао"=Крамеру.

Для любой несмещённой оценки $0 < e(\theta) \leq 1$. Если $e(\theta) = 1$, то
$\tilde{\theta}_n$ называется эффективной по Рао"=Крамеру, следовательно, просто эффективна.
\bigskip

\textbf{Построение эффективной по Рао-Крамеру
оценки для пуассоновского распределения:}
\begin{center}
    \includegraphics[width=170mm]{2_3_6.png}
\end{center}

\section{Методы построения оценок}

\subsection{Метод моментов. Пример (биномиальное распределение)}

\textbf{Определение:}
\smallskip

Начальным теоретическим моментом k"=го порядка называется 

$m_k = M\xi^k$, если $M|\xi|^k < +\infty$.
\bigskip

\textbf{Определение:}
\smallskip

Центральным теоретическим моментом k"=го порядка называется

$\mu_k = M(\xi - M\xi)^k$, если $M|\xi| < + \infty$.
\bigskip

\textbf{Определение:}
\smallskip

Выборочным начальным моментом k"=го порядка называется

$\tilde{m}_k = \frac{1}{n} \displaystyle\sum^n_{i = 1} X^k_i$.
\bigskip

\textbf{Определение:}
\smallskip

Выборочным центральным моментом k"=го порядка называется

$\tilde{\mu}_k = \frac{1}{n} \displaystyle\sum^n_{i = 1} (X_i - \overline{x})^k$.
\bigskip

\textbf{Метод моментов:}
\smallskip

Метод моментов состоит в том, что за оценку параметров принимается решение системы
уравнений:

\begin{equation}
    \begin{cases}
        m_k = \tilde{m}_k \\
        \mu_k = \tilde{\mu}_k
    \end{cases}
\end{equation}

\textbf{Пример (биномиальное распределение):}
\smallskip

\begin{center}
    \includegraphics[width=170mm]{2_4_1.png}
\end{center}

\subsection{Функция правдоподобия. Метод максимального правдоподобия. Пример (биномиальное распределение)}

\textbf{Определение:}
\smallskip

Пусть $X_1, \dots, X_n \sim L_{\xi} (x, \theta)$, причём $f_{\xi} (x, \theta)$~---
известна, а $\theta$ неизвестен.

Функцией правдоподобия называют

$L(\overline{X}_n, \theta) = \displaystyle\prod^n_{i = 1} f(X_i, \theta) = f(X_1, \theta)
* \dots * f(X_n, \theta)$.
\bigskip

\textbf{Метод максимального правдоподобия:}
\smallskip

Рассмотрим $\ln L(\overline{X}_n, \theta) = \displaystyle\sum^n_{i = 1} \ln f (X_i, \theta)$.
\smallskip

Метод максимального правдоподобия состоит в том, что за оценку параметра $\theta$
принимается точка максимума функции правдоподобия.
\smallskip

Алгоритм:
\begin{enumerate}
    \item Построить функцию $L(\overline{X}_n, \theta)$;
    \item Взять её логарифм $\ln L (\overline{X}_n, \theta)$;
    \item {$\frac{\partial \ln L (\overline{X}_n, \theta)}{\partial \theta} = 0
    \Rightarrow \theta$;}
    \item {Если значение второй производной строго меньше нуля, то оценка параметра
    $\tilde{\theta}$ и есть $\theta_0$.
    
    $$\frac{\partial^2 \ln L (\overline{X}_n, \theta)}{\partial \theta^2} |_{\theta_0}
    < 0, \text{то} \;\; \tilde{\theta} = \theta_0$$}
\end{enumerate}

\textbf{Пример (биномиальное распределение):}
\smallskip

\begin{center}
    \includegraphics[width=170mm]{2_4_2.png}
\end{center}

\section{Доврительное оценивание}

\subsection{Поставнока задачи, определение доверительного интервала,
доверительная вероятность}

\begin{center}
    \includegraphics[width=170mm]{2_5_2.png}
\end{center}

\subsection{Лемма Фишера}

\begin{center}
    \includegraphics[width=170mm]{2_5_1.png}
\end{center}

\subsection{Распределения Стьюдента, хи"=квадрат, формулы построения этих случайных
величин}

\textbf{Определение:}
\begin{center}
    \includegraphics[width=170mm]{2_5_3.png}
\end{center}

\textbf{Определение:}
\begin{center}
    \includegraphics[width=170mm]{2_5_4.png}
\end{center}

\subsection{Построение доверительных интервалов для параметра $a$ нормального
распределения при известном и неизвестном $\sigma$}

\textbf{При известном $\sigma$:}
\begin{center}
    \includegraphics[width=170mm]{2_5_5.png}
\end{center}

\textbf{При неизвестном $\sigma$:}
\begin{center}
    \includegraphics[width=170mm]{2_5_6.png}
\end{center}

\section{Проверка статистических гипотез}

\subsection{Определение статистической гипотезы, критерия,
ошибок первого и второго рода, уровня значимости, мощности критерия,
критической области.}

\begin{center}
    \includegraphics[width=170mm]{2_6_1.png}
\end{center}

\section{Регрессионный анализ}

\subsection{Постановка задачи}

\textbf{подготовительная инфоррмация:}
\smallskip

Выборочные средние имеют вид:

$$\overline{x} = \frac{1}{n} \sum^n_{i = 1} X_i, \;\;\; \overline{y} = \frac{1}{n} \sum^n_{i = 1} Y_i$$

Выборочные дисперсии (смещённые) имеют вид:

$$S^2_{X} = \frac{1}{n} \sum^n_{i = 1} (X_i - \overline{x})^2, \;\;\;
S^2_{Y} = \frac{1}{n} \sum^n_{i = 1} (Y_i - \overline{y})^2.$$

Величины $S_X = \sqrt{S^2_x}$ и $S_Y = \sqrt{S^2_Y}$ представляют собой выборочные
средние квадратические отклонения (смещённые) величин $\xi$ и $\eta$.
\bigskip

Выборочное среднее (совместное) есть величина:

$$\overline{xy} = \frac{1}{n} \sum^n_{i = 1} X_i Y_i$$

Коэффициент корреляции (выборочный):

$$r_{\text{выб}} (\text{дальше} \;\;\; r_v) = \frac{\overline{xy} - \overline{x} * \overline{y}}{\sqrt{S^2_x} \sqrt{S^2_y}}$$

\textbf{Постановка задачи:}
\smallskip

Пусть производятся наблюдения некоторого процесса, который описывается двумя случайными
величинами $(\xi, \eta)$.

Тогда результаты наблюдений представляют собой двумерную выборку

$(X_1, Y_1), \dots, (X_n, Y_n)$.

Множество точек с координатами $(X_i, Y_i)$ называют полем корреляции или диаграммой
рассеяния. По виду поля корреляции делают предположение о связи между величинами
$\xi$ и $\eta$.
\begin{center}
    \includegraphics[width=170mm]{2_7_1.png}
\end{center}

\subsection{Построение выборочного уравнения регрессии}

\begin{center}
    \includegraphics[width=170mm]{2_7_2.png}
    \includegraphics[width=170mm]{2_7_3.png}
    \includegraphics[width=170mm]{2_7_4.png}
    \includegraphics[width=170mm]{2_7_5.png}
\end{center}

\subsection{Выборочный коэффициент корреляции, его свойства}

\textbf{Определения:}
\smallskip

$$r_{\text{выб}} (\text{дальше} \;\;\; r_v) = \frac{\overline{xy} - \overline{x} * \overline{y}}{\sqrt{S^2_x} \sqrt{S^2_y}}$$

\begin{center}
    \includegraphics[width=170mm]{2_7_6.png}
\end{center}

\subsection{Проверка гипотезы о значимости выборочного коэффициента корреляции}

\begin{center}
    \includegraphics[width=170mm]{2_7_7.png}
\end{center}

\subsection{Теорема Гаусса"=Маркова}

\begin{center}
    \includegraphics[width=170mm]{2_7_8.png}
\end{center}

\subsection{Статические свойства МНК оценок}

\begin{center}
    \includegraphics[width=170mm]{2_7_9.png}
\end{center}

\subsection{Анализ вариации}

\begin{center}
    \includegraphics[width=170mm]{2_7_10.png}
\end{center}

\end{document}